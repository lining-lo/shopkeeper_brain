# 环境配置与服务部署指南

> 本文档详细介绍掌柜智库项目的环境配置与中间件服务部署，包括 Python 环境搭建、Docker 安装、各中间件部署配置及验证方法。

---

## 1. Python 项目环境

### 1.1 虚拟环境创建

#### 方式一：使用 venv（推荐）

```bash
# 1.进入项目目录
cd knowledge

# 2. 创建虚拟环境
python -m venv .venv

# 3. 激活虚拟环境
# Windows (CMD)
.venv\Scripts\activate.bat

# Windows (PowerShell)
.venv\Scripts\Activate.ps1

# Linux / macOS
source .venv/bin/activate
```

#### 方式二：使用 conda

```bash
# 1. 创建 conda 环境
conda create -n knowledge python=3.10 -y

# 2. 激活环境
conda activate knowledge
```

### 1.2 依赖安装

项目依赖定义在 `requirements.txt` 中：

```txt
# 核心依赖，执行下载需要删除中文注释，否则报编码错误   
minio                    # 对象存储客户端
langchain-openai         # LangChain OpenAI 集成
langgraph                # 工作流编排框架
grandalf                 # 图布局算法（LangGraph 依赖）
pymilvus[model]          # Milvus 模型支持
sentence-transformers    # 句向量模型
pymongo                  # MongoDB 驱动
mineru[all]              # PDF 解析工具
openai-agents            # OpenAI Agent 框架
```

**安装命令（使用国内镜像）：**

```bash
#直接用pip安装比较慢，串行安装 2.7G大小 35分钟
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

#【或】  使用清华或者阿里镜像安装，先按uv,使用vu并行安装，速度更快
pip install uv -i https://mirrors.aliyun.com/pypi/simple
uv pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
uv pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple

# 安装 PyTorch单独从官网安装 【老师cu118】   2.8G大小   14分钟  使用--extra-index-url参数，不要使用-i
#pip install torch torchvision -i https://download.pytorch.org/whl/cu118
pip install torch torchvision --extra-index-url https://download.pytorch.org/whl/cu118
```

**常见安装问题：**

| 问题             | 解决方案                                 |
| ---------------- | ---------------------------------------- |
| PyTorch 安装失败 | 检查 CUDA 版本，选择对应的 whl 源        |
| mineru 安装慢    | 使用 `--no-deps` 先装核心包              |
| 编译错误         | 安装 Visual Studio Build Tools (Windows) |

### 1.3 环境变量配置

项目使用 `.env` 文件管理环境变量，需在 `knowledge/` 目录下创建：

```bash
# 复制模板（如有）
copy .env.example .env

# 或手动创建
```

**完整 .env 配置示例：**

```ini
# ====================
# Model Source & Cache
# ====================
# 模型来源（ModelScope / HuggingFace）
MINERU_MODEL_SOURCE=modelscope
# ModelScope 离线模式（1=启用）
MODELSCOPE_OFFLINE=1
# ModelScope 模型缓存路径
MODELSCOPE_CACHE=D:/ai_models/modelscope_cache
# Hugging Face 缓存路径
HF_HOME=D:/ai_models/huggingface_cache
# 临时文件根目录
MD_ROOT_DIR=./temp-files/

# mineru api以及url处理地址！
MINERU_API_TOKEN=xxx
MINERU_BASE_URL=https://mineru.net/api/v4

# ====================
# OpenAI / LLM API (DashScope compatible)
# ====================
# API 密钥（兼容 OpenAI 格式）
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# API 基础地址（阿里云 DashScope）
OPENAI_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
# 默认 LLM 模型
LLM_DEFAULT_MODEL=qwen-flash
# 默认温度参数（0-1，越低越稳定）
LLM_DEFAULT_TEMPERATURE=0.1
# 视觉语言模型
VL_MODEL=qwen3-vl-flash
# 商品名识别模型
ITEM_MODEL=qwen-flash

# ====================
# BGE Embedding Models
# ====================
# BGE-M3 模型本地路径
BGE_M3_PATH=D:\ai_models\modelscope_cache\models\BAAI\bge-m3
# BGE-M3 模型名称
BGE_M3=BAAI/bge-m3
# 嵌入模型运行设备
BGE_DEVICE=cuda:0
## 是否使用半精度
#BGE_FP16=True
# BGE-M3是否开启FP16半精度推理，1=开启（GPU加速更高效），0=关闭（兼容低版本GPU/CPU）
BGE_FP16=1
# BGE 重排序模型路径
BGE_RERANKER_LARGE=D:\ai_models\modelscope_cache\models\BAAI\bge-reranker-large
# 重排序模型设备
BGE_RERANKER_DEVICE=cuda:0
# 重排序模型半精度
BGE_RERANKER_FP16=1
# 项目名称诊断标志
ITEM_NAME_DIAG=1

# ====================
# Embedding General
# ====================
# 嵌入向量维度（OpenAI text-embedding-v4）
EMBEDDING_DIM=1536
# 嵌入模型名称
EMBEDDING_MODEL=text-embedding-v4

# ====================
# Vector Database (Milvus)
# ====================
# Milvus 连接地址
MILVUS_URL=http://192.168.6.150:19530
# 知识库切片集合名
CHUNKS_COLLECTION=kb_chunks_v1
# 商品名称集合名
ITEM_NAME_COLLECTION=kb_item_names_v1
# 相似度度量方式
MILVUS_METRIC_TYPE=COSINE
# 最小余弦相似度阈值
MILVUS_MIN_COSINE_SCORE=0.75
# Milvus认证配置（如果启用了认证）
# MILVUS_USER=root
# MILVUS_PASSWORD=Milvus

# ====================
# Document Database (MongoDB)
# ====================
# MongoDB 连接 URL
MONGO_URL=mongodb://192.168.6.150:27017
# 数据库名
MONGO_DB_NAME=kb001

# ====================
# Object Storage (MinIO)
# ====================
# MinIO 服务端点
MINIO_ENDPOINT=192.168.6.150:9000
# 访问密钥
MINIO_ACCESS_KEY=minioadmin
# 私有密钥
MINIO_SECRET_KEY=minioadmin
# 存储桶名称
MINIO_BUCKET_NAME=knowledge-base-files

# ====================
# Other / MCP Services
# ====================
# MCP Web 搜索服务地址
#MCP_DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/api/v1/mcps/WebSearch/sse
MCP_DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/api/v1/mcps/WebSearch/mcp
```

---

## 2. 配置文件说明

### 2.1 配置项分类

| 类别         | 配置项                               | 说明                |
| ------------ | ------------------------------------ | ------------------- |
| **模型缓存** | `MODELSCOPE_CACHE`, `HF_HOME`        | 大模型本地缓存路径  |
| **LLM API**  | `OPENAI_API_KEY`, `OPENAI_API_BASE`  | 大语言模型 API 配置 |
| **向量模型** | `BGE_M3_PATH`, `BGE_DEVICE`          | BGE 嵌入模型配置    |
| **数据库**   | `MILVUS_URL`, `MONGO_URL`            | 各数据库连接配置    |
| **对象存储** | `MINIO_ENDPOINT`, `MINIO_ACCESS_KEY` | MinIO 存储配置      |

### 2.2 数据库连接配置

#### 2.2.1 Milvus 向量数据库

```python
# knowledge/utils/milvus_utils.py 中的连接逻辑
from pymilvus import connections

connections.connect(
    alias="default",
    uri=os.getenv("MILVUS_URL", "http://192.168.6.150:19530")
)
```

#### 2.2.2 MongoDB 文档数据库

```python
# knowledge/utils/mongo_history_utils.py 中的连接逻辑
from pymongo import MongoClient

client = MongoClient(os.getenv("MONGO_URL", "mongodb://192.168.6.150:27017"))
db = client[os.getenv("MONGO_DB_NAME", "kb001")]
```

### 2.3 API 密钥配置

**阿里云 DashScope 申请步骤：**

<img src="D:\project\shopkeeper_brain\notes\images\9.dashscope API KEY申请.jpg" style="zoom:67%;" />

**支持的模型：**

| 模型名称            | 用途               | 计费     |
| ------------------- | ------------------ | -------- |
| `qwen-flash`        | 通用对话、知识抽取 | 按 token |
| `qwen3-vl-flash`    | 图像理解           | 按 token |
| `text-embedding-v4` | 文本向量化         | 按 token |

### 2.4 路径配置

```ini
# 临时文件目录（相对于项目根目录）
MD_ROOT_DIR=./temp-files/

# 模型缓存目录（建议使用绝对路径）
MODELSCOPE_CACHE=D:/ai_models/modelscope_cache
HF_HOME=D:/ai_models/huggingface_cache
BGE_M3_PATH=D:/ai_models/modelscope_cache/models/BAAI/bge-m3
BGE_RERANKER_LARGE=D:/ai_models/modelscope_cache/models/BAAI/bge-reranker-large
```

---

## 3. 基础环境准备

### 3.1 Docker 安装

#### 3.1.1 Linux

参考docker安装

#### 3.1.2 Windows

1. **下载 Docker Desktop**

   - 访问 https://www.docker.com/products/docker-desktop/
   - 下载 Windows 版本

2. **安装前提条件**

   - 启用 WSL 2（Windows 10 2004+）
   - 或启用 Hyper-V

3. **启用 WSL 2**

   ```powershell
   # 以管理员身份运行 PowerShell
   wsl --install
   wsl --set-default-version 2
   ```

4. **安装 Docker Desktop**

   - 运行安装程序
   - 选择 "Use WSL 2 instead of Hyper-V"
   - 完成安装后重启

5. **验证安装**

   ```powershell
   docker --version
   docker compose version
   ```

### 3.2 配置 Docker 镜像加速

1./etc/docker/daemon.json 配置镜像加速网址

### 3.3 Docker Compose 验证

```bash
# 检查版本
docker compose version

# 应输出类似：
# Docker Compose version v2.x.x
```

---

## 4. 中间件部署

### 4.1 Milvus 向量数据库

Milvus Standalone 模式需要 **Etcd** 和 **MinIO** 作为依赖服务。

#### 4.1.1 Docker Compose 配置（docker-compose.yml）

```yaml
version: '3.5'

services:
  # ==================== Etcd ====================
  etcd:
    container_name: milvus-etcd
    image: quay.io/coreos/etcd:v3.5.25
    environment:
      - ETCD_AUTO_COMPACTION_MODE=revision
      - ETCD_AUTO_COMPACTION_RETENTION=1000
      - ETCD_QUOTA_BACKEND_BYTES=4294967296
      - ETCD_SNAPSHOT_COUNT=50000
    volumes:
      - ${DOCKER_VOLUME_DIRECTORY:-.}/volumes/etcd:/etcd
    command: etcd -advertise-client-urls=http://etcd:2379 -listen-client-urls http://0.0.0.0:2379 --data-dir /etcd
    healthcheck:
      test: ["CMD", "etcdctl", "endpoint", "health"]
      interval: 30s
      timeout: 20s
      retries: 3
    restart: always

  # ==================== MinIO ====================
  minio:
    container_name: milvus-minio
    image: minio/minio:RELEASE.2024-12-18T13-15-44Z
    environment:
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
    ports:
      - "9001:9001"
      - "9000:9000"
    volumes:
      - ${DOCKER_VOLUME_DIRECTORY:-.}/volumes/minio:/minio_data
    command: minio server /minio_data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
    restart: always

  # ==================== Milvus ====================
  standalone:
    container_name: milvus-standalone
    image: milvusdb/milvus:v2.5.5
    command: ["milvus", "run", "standalone"]
    security_opt:
      - seccomp:unconfined
    environment:
      ETCD_ENDPOINTS: etcd:2379
      MINIO_ADDRESS: minio:9000
      MQ_TYPE: rocksmq
    volumes:
      - ${DOCKER_VOLUME_DIRECTORY:-.}/volumes/milvus:/var/lib/milvus
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9091/healthz"]
      interval: 30s
      start_period: 90s
      timeout: 20s
      retries: 3
    ports:
      - "19530:19530"
      - "9091:9091"
    restart: always
    depends_on:
      - etcd
      - minio

  # ==================== Attu (Milvus GUI) ==========
  attu:
    container_name: attu
    image: zilliz/attu:v2.5.10
    environment:
      - MILVUS_URL=standalone:19530
      - ATTU_LOG_LEVEL=info
      - SERVER_PORT=7000
    ports:
      - "7000:7000"
    restart: always
    depends_on:
      - standalone

  # ==================== MongoDB ====================
  mongodb:
    container_name: mongodb
    image: mongo:7.0
    ports:
      - "27017:27017"
    volumes:
      - ${DOCKER_VOLUME_DIRECTORY:-.}/volumes/mongodb/data:/data/db
      - ${DOCKER_VOLUME_DIRECTORY:-.}/volumes/mongodb/config:/data/configdb
    restart: always
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  default:
    name: milvus
```

#### 4.1.2 启动

```bash
# 启动所有容器服务
docker compose -f docker-compose.yml up -d
```

#### 4.1.3 控制台访问

- Attu连接Milvus无账号密码

- 地址：http://localhost:7000   

---

### 4.2 MinIO 对象存储

MinIO 在上述 Milvus 配置中已包含，同时也作为项目的文件存储服务。

#### 4.2.1 端口说明

| 端口   | 用途             |
| ------ | ---------------- |
| `9000` | S3 兼容 API 端口 |
| `9001` | Web 控制台端口   |

#### 4.2.2 Web 控制台访问

- 地址：http://localhost:9001
- 用户名：`minioadmin`
- 密码：`minioadmin`

#### 4.2.3 创建存储桶

首次使用需创建项目存储桶：

```bash
# 通过 Web 控制台创建
# 访问 http://localhost:9001 → Buckets → Create Bucket → 输入 "knowledge-base-files"
```

---

### 4.3 MongoDB 文档数据库

#### 4.3.1 Docker Compose 配置

```yaml
mongodb:
  container_name: mongodb
  image: mongo:7.0
  environment:
    # 可选：设置 root 账号（生产环境建议配置）
    # MONGO_INITDB_ROOT_USERNAME: admin
    # MONGO_INITDB_ROOT_PASSWORD: password
  ports:
    - "27017:27017"
  volumes:
    - ./volumes/mongodb/data:/data/db
    - ./volumes/mongodb/config:/data/configdb
  restart: always
  healthcheck:
    test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
    interval: 30s
    timeout: 10s
    retries: 3
```

#### 4.3.2 启动与验证

```bash
# 启动 MongoDB
docker compose -f docker-compose.yml up -d

# 连接测试
docker exec -it mongodb mongosh

# 在 mongosh 中验证
> show dbs
> use kb001
> db.stats()
```

直接安装客户端UI工具：mongodb-compass-1.39.3-win32-x64.exe

---

## 5. 一键部署方案

### 5.1 完整 docker-compose.yml



### 5.2 一键启动所有服务

```bash
# 启动所有服务
docker compose -f docker-compose.yml up -d

# 删除所有服务
docker compose -f docker-compose.yml down

# 停止所有服务
docker compose -f docker-compose.yml stop

# 启动所有服务
docker compose -f docker-compose.yml start

# 查看服务状态
docker ps -a

# 查看所有日志
docker logs -f 容器名
```

### 5.3 服务启动顺序

```
┌─────────────────────────────────────────────────────────────┐
│                              服务启动顺序                                    │
└─────────────────────────────────────────────────────────────┘

        ┌─────────────┐     ┌─────────────┐
        │    etcd     │     │    minio    │
        │  (元数据)    │     │  (对象存储)  │
        └──────┬──────┘     └──────┬──────┘
               │                   │
               └─────────┬─────────┘
                         │
                         ▼
              ┌─────────────────┐
              │   standalone    │
              │   (Milvus)      │
              └────────┬────────┘
                       │
                       ▼
              ┌─────────────────┐
              │      attu       │
              │   (Milvus GUI)  │
              └─────────────────┘


        ┌─────────────────┐
        │    mongodb      │
        │   (独立启动)     │
        └─────────────────┘
```

---

## 6. 验证安装

**各服务连接测试脚本**

创建 `test_connections.py`：

```python
#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""
服务连接测试脚本
"""
import os
from dotenv import load_dotenv

# 加载环境变量
load_dotenv()


def test_milvus():
    """测试 Milvus 连接"""
    print("测试 Milvus 连接...")
    try:
        from pymilvus import MilvusClient
        client = MilvusClient(
            uri=os.getenv("MILVUS_URL", "http://192.168.6.150:19530"),
            token=f"{os.getenv('MILVUS_USER', 'root')}:{os.getenv('MILVUS_PASSWORD', 'Milvus')}")
        version = client.get_server_version()
        print(f"  ✓ Milvus 连接成功，版本: {version}")
        client.close()
        return True
    except Exception as e:
        print(f"  ✗ Milvus 连接失败: {e}")
        return False


def test_mongodb():
    """测试 MongoDB 连接"""
    print("测试 MongoDB 连接...")
    try:
        from pymongo import MongoClient
        client = MongoClient(
            os.getenv("MONGO_URL", "mongodb://192.168.6.150:27017"),
            serverSelectionTimeoutMS=5000,
            #username=os.getenv("MONGO_USER", "admin"),  # 新增：读取账号
            #password=os.getenv("MONGO_PASSWORD", "123456"),  # 新增：读取密码
        )
        # 触发实际连接
        client.admin.command('ping')
        db_names = client.list_database_names()
        print(f"  ✓ MongoDB 连接成功，数据库列表: {db_names}")
        client.close()
        return True
    except Exception as e:
        print(f"  ✗ MongoDB 连接失败: {e}")
        return False


def test_minio():
    """测试 MinIO 连接"""
    print("测试 MinIO 连接...")
    try:
        from minio import Minio
        client = Minio(
            os.getenv("MINIO_ENDPOINT", "192.168.6.150:9000"),
            access_key=os.getenv("MINIO_ACCESS_KEY", "minioadmin"),
            secret_key=os.getenv("MINIO_SECRET_KEY", "minioadmin"),
            secure=False
        )
        buckets = client.list_buckets()
        bucket_names = [b.name for b in buckets]
        print(f"  ✓ MinIO 连接成功，存储桶: {bucket_names}")
        return True
    except Exception as e:
        print(f"  ✗ MinIO 连接失败: {e}")
        return False


def main():
    print("=" * 50)
    print("掌柜智库 - 服务连接测试")
    print("=" * 50)

    results = {
        "Milvus": test_milvus(),
        "MongoDB": test_mongodb(),
        "MinIO": test_minio(),
    }

    print("\n" + "=" * 50)
    print("测试结果汇总")
    print("=" * 50)

    all_passed = True
    for service, passed in results.items():
        status = "✓ 通过" if passed else "✗ 失败"
        print(f"  {service}: {status}")
        if not passed:
            all_passed = False

    print("=" * 50)
    if all_passed:
        print("所有服务连接正常！")
    else:
        print("存在服务连接失败，请检查配置。")

    return all_passed


if __name__ == "__main__":
    import sys

    sys.exit(0 if main() else 1)


"""
==================================================
掌柜智库 - 服务连接测试
==================================================
测试 Milvus 连接...
  ✓ Milvus 连接成功，版本: pkg/v2.5.5
测试 MongoDB 连接...
  ✓ MongoDB 连接成功，数据库列表: ['admin', 'config', 'kb', 'local', 'users']
测试 MinIO 连接...
  ✓ MinIO 连接成功，存储桶: ['knowledge-base-files']

==================================================
测试结果汇总
==================================================
  Milvus: ✓ 通过
  MongoDB: ✓ 通过
  MinIO: ✓ 通过
==================================================
所有服务连接正常！

"""
```

运行测试：

```bash
python test_connections.py
```

---

## 7. 故障排查

### 7.1 Docker 常见问题

| 问题                                  | 原因                   | 解决方案                                         |
| ------------------------------------- | ---------------------- | ------------------------------------------------ |
| `Cannot connect to the Docker daemon` | Docker 服务未启动      | `sudo systemctl start docker`                    |
| `permission denied`                   | 当前用户不在 docker 组 | `sudo usermod -aG docker $USER && newgrp docker` |
| `no space left on device`             | 磁盘空间不足           | `docker system prune -a` 清理无用镜像            |
| 镜像拉取超时                          | 网络问题               | 配置镜像加速器                                   |

### 7.2 端口冲突解决

```bash
# 查看端口占用
# Linux/macOS
lsof -i :19530
netstat -tlnp | grep 19530

# Windows
netstat -ano | findstr :19530

# 解决方案
# 1. 停止占用进程
# 2. 或修改 docker-compose.yml 中的端口映射
```

**常用端口一览：**

| 服务    | 默认端口 | 用途       |
| ------- | -------- | ---------- |
| Milvus  | 19530    | gRPC 连接  |
| Milvus  | 9091     | 健康检查   |
| MongoDB | 27017    | 数据库连接 |
| MinIO   | 9000     | S3 API     |
| MinIO   | 9001     | Web 控制台 |
| Attu    | 7000     | Milvus GUI |

### 7.3 权限问题

```bash
# 数据卷权限问题（Linux）
sudo chown -R 1000:1000 volumes/

# MongoDB 权限
sudo chown -R 999:999 volumes/mongodb/
```

### 7.4 网络问题

```bash
# 检查 Docker 网络
docker network ls
docker network inspect milvus

# 容器间通信测试
docker exec milvus-standalone ping -c 3 etcd
docker exec milvus-standalone ping -c 3 minio

# 重建网络
docker compose down
docker network rm milvus
docker compose up -d
```

### 7.5 资源不足问题

**内存不足症状：**

- Milvus 频繁重启
- OOM Killed 错误

**解决方案：**

```bash
# 检查容器资源使用
docker stats

# 限制容器内存（docker-compose.yml）
services:
  standalone:
    deploy:
      resources:
        limits:
          memory: 4G
        reservations:
          memory: 2G
```

### 7.6 日志查看

```bash
# 查看特定服务日志
docker compose logs -f milvus-standalone
docker compose logs -f mongodb

# 查看最近 100 行
docker compose logs --tail=100 milvus-standalone

# 导出日志到文件
docker compose logs > all_logs.txt
```

---

## 附录

### A. 服务端口速查表

| 服务     | 端口  | 协议 | 用途       |
| -------- | ----- | ---- | ---------- |
| Milvus   | 19530 | gRPC | 数据库连接 |
| Milvus   | 9091  | HTTP | 健康检查   |
| MongoDB  | 27017 | TCP  | 数据库连接 |
| MinIO    | 9000  | HTTP | S3 API     |
| MinIO    | 9001  | HTTP | Web 控制台 |
| Attu     | 7000  | HTTP | Milvus GUI |
| 导入服务 | 8000  | HTTP | 文件上传   |
| 查询服务 | 8001  | HTTP | 知识查询   |

### B. 环境变量快速参考

```ini
# 必填项
OPENAI_API_KEY=sk-xxx
OPENAI_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
MILVUS_URL=http://192.168.6.150:19530
MONGO_URL=mongodb://192.168.6.150:27017

# 可选项（有默认值）
LLM_DEFAULT_MODEL=qwen-flash
BGE_DEVICE=cuda:0
MINIO_ENDPOINT=192.168.6.150:9000
```

### C. 常用命令速查

```bash
# Docker Compose
docker compose up -d                  # 启动所有服务
docker compose down                   # 停止所有服务
docker compose ps                     # 查看服务状态
docker compose logs -f [service]      # 查看日志
docker compose restart [service]      # 重启服务


# 进入容器
docker exec -it mongodb mongosh
```

---